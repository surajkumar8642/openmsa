# OpenMSA Specification v1alpha1

## Summary

- Modular modules: identity, policy, index, storage, gateway.
- Bearer token authorization per request.
- Opaque identifiers and claim-based indexing.
- Write-only inbox semantics.

## API Contracts

See `apps/example-server/Program.cs` for MVP endpoints and `spec` JSON schema artifacts for resource envelopes and manifests.
